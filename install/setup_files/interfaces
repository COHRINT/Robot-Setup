source-directory /etc/network/interfaces.d

auto lo
iface lo inet loopback

allow-hotplug eth0
iface eth0 inet dhcp

auto wlan0
allow-hotplug wlan0
iface wlan0 inet dhcp
	wpa_ssid "RECUV-VICON"
	wpa_psk "THE Big ONE"
iface default inet dhcp
	wpa_psk "BIGGY"
iface default inet dhcp
